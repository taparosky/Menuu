@model CartViewModel

@if(Model.Cart.CartItems.Count > 0)
{
    <a asp-controller="Cart" asp-action="Index">
        <span><img src="~/img/cart-outline.png" width="20px" height="20px"/></span>
        <span style="color:white">
            @Model.Cart.CartItems.Count
        </span>
    </a>
}